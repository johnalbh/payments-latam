// Public API — only the symbols re-exported here are part of the stable surface.
// Everything else in `src/` is an implementation detail and may change without notice.

export { LIB_VERSION } from './version';
